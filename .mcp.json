{
  "mcpServers": {
    "obsidian": {
      "type": "http",
      "url": "${PROTOCOL}://${HOST}:${PORT}/mcp/",
      "headers": {
        "Authorization": "Bearer ${OBSIDIAN_API_KEY}"
      }
    }
  }
}